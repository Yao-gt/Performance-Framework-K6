name: K6 Performance Tests

on:
  push:
    branches: [ main, develop ]
  workflow_dispatch: 

jobs:
  deploy-k6-test-on-docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build the docker-compose up
        timeout-minutes: 2
        run: docker compose -f docker/docker-compose.yml up -d

      - name: Wait for Frontend (Healthcheck)
        run: |
          echo "Waiting for port 8080 to open..."
          timeout 60s bash -c 'until nc -z localhost 8080; do echo "8080 still closed... retrying"; sleep 5; done' || true
          
          if ! nc -z localhost 8080; then
            echo "ERROR: The port 8080 never opened. Showing frontend logs for debug:"
            docker logs shop-frontend
            exit 1
          fi

      - name: Create reports directory
        run: mkdir -p reports

      - name: Run k6 api test
        run: |
          docker run --rm --network host \
            --user $(id -u):$(id -g) \
            -v $(pwd):/apps \
            -w /apps \
            -e BASE_URL="http://127.0.0.1:8080" \
            -e TEST_NAME="api_test" \
            grafana/k6:latest \
            run src/scripts/api/test.js \
            --out influxdb=http://127.0.0.1:8086/k6
            
      - name: Upload Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: k6-reports-history
          path: reports/*.html
          retention-days: 3

      - name: Run k6 ui performance test
        run: |
          docker run --rm --network host \
            --shm-size=2gb \
            --user $(id -u):$(id -g) \
            -v $(pwd):/apps \
            -w /apps \
            -e BASE_URL="http://127.0.0.1:8080" \
            -e TEST_NAME="ui_test" \
            -e K6_BROWSER_ENABLED=true \
            grafana/k6:latest-with-browser \
            run src/scripts/browser/ui-performance.js \
            --out influxdb=http://127.0.0.1:8086/k6

      - name: Upload Browser Evidences
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: browser-performance-evidences
          path: browser_evidences/
          retention-days: 3

      - name: Build the docker compose down
        if: always()
        run: docker compose -f docker/docker-compose.yml down
